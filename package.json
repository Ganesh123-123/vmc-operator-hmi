{
  "name": "vmc-operator-hmi",
  "version": "1.0.0",
  "description": "VMC Operator HMI – Startup Guidance (Primeform Labs Technical Assignment)",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend && npm run db:generate --prefix backend && npm run db:push --prefix backend && npm run db:seed --prefix backend && npm run build --prefix backend",
    "render-build": "npm install --include=dev && npm install --include=dev --prefix frontend && npm run build --prefix frontend && npm install --include=dev --prefix backend && npm run db:generate --prefix backend && npm run db:push --prefix backend && npm run db:seed --prefix backend && npm run build --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "db:setup": "npm run db:setup --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "test": "npm run test --prefix backend",
    "start": "node backend/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": [
    "vmc",
    "hmi",
    "cnc",
    "operator-interface",
    "industrial-ui",
    "react",
    "express",
    "prisma",
    "sqlite"
  ],
  "author": "Antigravity Engineer",
  "license": "ISC"
}
